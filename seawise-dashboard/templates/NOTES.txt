🎉 Seawise Backup Dashboard foi instalado com sucesso!

📦 Release: {{ .Release.Name }}
🔖 Namespace: {{ include "seawise-dashboard.namespace" . }}
🏷️  Versão: {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 INFORMAÇÕES DA INSTALAÇÃO:

1. Verificar status do deployment:

   kubectl get deployment -n {{ include "seawise-dashboard.namespace" . }}

2. Verificar pods:

   kubectl get pods -n {{ include "seawise-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "seawise-dashboard.name" . }}

3. Ver logs:

   kubectl logs -n {{ include "seawise-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "seawise-dashboard.name" . }} -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 COMO ACESSAR O DASHBOARD:

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  ✅ Ingress habilitado: https://{{ .host }}
{{- end }}
{{- else if .Values.route.enabled }}
  ✅ OpenShift Route habilitado.

  Obtenha a URL com:
  {{- if eq .Capabilities.KubeVersion.Major "1" }}
  kubectl get route -n {{ include "seawise-dashboard.namespace" . }} {{ include "seawise-dashboard.fullname" . }}
  {{- else }}
  oc get route -n {{ include "seawise-dashboard.namespace" . }} {{ include "seawise-dashboard.fullname" . }}
  {{- end }}
{{- else }}
  ℹ️  Ingress/Route não habilitado. Use port-forward para acessar:

  kubectl port-forward -n {{ include "seawise-dashboard.namespace" . }} svc/{{ include "seawise-dashboard.fullname" . }} 8080:{{ .Values.service.port }}

  Acesse: http://localhost:8080
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️  CONFIGURAÇÃO:

• Velero Namespace: {{ .Values.app.veleroNamespace | default "auto-detectado" }}
• Timezone: {{ .Values.app.timezone }}
• Persistência: {{ if .Values.persistence.enabled }}✅ Habilitada ({{ .Values.persistence.size }}){{ else }}❌ Desabilitada{{ end }}
• RBAC: {{ if .Values.rbac.create }}✅ Criado{{ else }}❌ Não criado{{ end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔐 PRÓXIMOS PASSOS:

1. Aguarde o pod estar em estado Running:

   kubectl wait --for=condition=ready pod -n {{ include "seawise-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "seawise-dashboard.name" . }} --timeout=300s

2. Acesse o dashboard (veja seção "COMO ACESSAR" acima)

3. No primeiro acesso, você será redirecionado para o Setup:
   - Configure o namespace do Velero (se necessário)
   - Crie o primeiro usuário administrador
   - Configure preferências

4. Faça login com as credenciais criadas

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTAÇÃO:

• README: https://github.com/shwcloudapp/seawise-backup/blob/main/helm-chart/seawise-dashboard/README.md
• Guia de Instalação: https://github.com/shwcloudapp/seawise-backup/blob/main/helm-chart/seawise-dashboard/INSTALL.md
• Instalação Velero: https://github.com/shwcloudapp/seawise-backup/blob/main/docs/instalacao_velero_completa.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 PRECISA DE AJUDA?

• GitHub Issues: https://github.com/shwcloudapp/seawise-backup/issues
• Troubleshooting: Consulte INSTALL.md seção "Troubleshooting"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 Bom uso do Seawise Backup Dashboard!
