🎉 Seawise Backup Dashboard installed successfully!

📦 Release: {{ .Release.Name }}
🔖 Namespace: {{ include "seawise-dashboard.namespace" . }}
🏷️  Version: {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 INSTALLATION INFO:

1. Check deployment status:

   kubectl get deployment -n {{ include "seawise-dashboard.namespace" . }}

2. Check pods:

   kubectl get pods -n {{ include "seawise-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "seawise-dashboard.name" . }}

3. View logs:

   kubectl logs -n {{ include "seawise-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "seawise-dashboard.name" . }} -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 HOW TO ACCESS THE DASHBOARD:

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  ✅ Ingress enabled: https://{{ .host }}
{{- end }}
{{- else if .Values.route.enabled }}
  ✅ OpenShift Route enabled.

  Get the URL with:
  {{- if eq .Capabilities.KubeVersion.Major "1" }}
  kubectl get route -n {{ include "seawise-dashboard.namespace" . }} {{ include "seawise-dashboard.fullname" . }}
  {{- else }}
  oc get route -n {{ include "seawise-dashboard.namespace" . }} {{ include "seawise-dashboard.fullname" . }}
  {{- end }}
{{- else }}
  ℹ️  Ingress/Route not enabled. Use port-forward to access:

  kubectl port-forward -n {{ include "seawise-dashboard.namespace" . }} svc/{{ include "seawise-dashboard.fullname" . }} 8080:{{ .Values.service.port }}

  Access: http://localhost:8080
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️  CONFIGURATION:

• Velero Namespace: {{ .Values.app.veleroNamespace | default "auto-detected" }}
• Timezone: {{ .Values.app.timezone }}
• Persistence: {{ if .Values.persistence.enabled }}✅ Enabled ({{ .Values.persistence.size }}){{ else }}❌ Disabled{{ end }}
• RBAC: {{ if .Values.rbac.create }}✅ Created{{ else }}❌ Not created{{ end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔐 NEXT STEPS:

1. Wait for pod to be Running:

   kubectl wait --for=condition=ready pod -n {{ include "seawise-dashboard.namespace" . }} -l app.kubernetes.io/name={{ include "seawise-dashboard.name" . }} --timeout=300s

2. Access the dashboard (see "HOW TO ACCESS" section above)

3. On first access, you'll be redirected to Setup:
   - Configure Velero namespace (if needed)
   - Create first admin user
   - Configure preferences

4. Login with the credentials you created

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION:

• Installation Guide: https://github.com/shwcloudapp/seawise-backup/blob/main/INSTALL.md
• Rancher Guide: https://github.com/shwcloudapp/seawise-backup/blob/main/RANCHER-INSTALL.md
• OpenShift Guide: https://github.com/shwcloudapp/seawise-backup/blob/main/OPENSHIFT-INSTALL.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 NEED HELP?

• Official Website: https://seawise.cloud
• GitHub Issues: https://github.com/shwcloudapp/seawise-backup/issues
• Troubleshooting: Check installation guides above

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 Happy backing up with Seawise!

For more information, visit: https://seawise.cloud
